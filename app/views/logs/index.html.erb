<div class="container-fluid main">
    <div class="row justify-content-center ">
        <div class="col-sm-10 devise-form rounded bg-dark text-warning">
            <% if user_signed_in? %>
                <%= current_user.logs.map{|log| log.regiment.exercise.name} %>
            <% end %>
        </div>
    </div>
</div> 
