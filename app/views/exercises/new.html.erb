<div class="container-fluid main">
    <div class="row justify-content-center ">
        <div class="col-sm-10 devise-form rounded bg-dark text-warning">
            <h1 class="text-light">Add Exercise</h1>

            <%= form_for(@exercise) do |f| %>
            <div class="form-group">
                <%= render 'shared/error_messages', :@profile => @exercise %>
                <%= f.label :name %>
                <%= f.text_field :name, autofocus: true, class: 'form-control'%>

                <div class="muscle_set">
                    <div class="muscle_set_item_container my-4 bg-secondary rounded" >
                        <div class="muscle_set_item rounded "  >
                            <%= f.fields_for :intentions do |intention| %>                    
                                <%= render 'intention_fields', f: intention, muscles: :muscles %>
                            <% end %>
                        </div>
                    </div>
                    <%= link_to_add_association 'Add Muscle', f, :intentions, {:class=>"btn btn-outline-warning my-4"}%>
                </div>
                <h6 class="text-danger">validation: must have muscle, must be unique muscle. must have primary</h6>
                <%= f.label :comments %>
                <%= f.text_field :comments, autofocus: true, class: 'form-control'%>

                <%= link_to 'Back', exercises_path, class: "btn btn-outline-warning" %>
                <%= f.submit "Save", class: "btn btn-outline-warning my-2" %>
                <% end %>
            </div>
        </div>
    </div>
</div>